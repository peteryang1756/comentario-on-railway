postgres:
  host: $POSTGRES_HOST
  port: $POSTGRES_PORT
  database: $POSTGRES_DATABASE
  username: $POSTGRES_USERNAME
  password: $POSTGRES_PASSWORD
  sslMode: require
  baseUrl: https://comentario-on-railway-1.onrender.com
idp:
  oidc:
    - id: sysport
      name: 雙龍體育
      url: https://admin-email-rouge.vercel.app
      scopes:
        - email
        - profile
        - openid
      disable: false
      key: $OIDC_KEY
      secret: $OIDC_SECRET
